#!/bin/bash
set -e

echo "=== CUSTOM ASSEMBLE START ==="

cd /tmp/src

chmod +x gradlew

./gradlew clean build -x test

echo "Fixing deployments path..."
rm -rf /deployments
mkdir -p /deployments

echo "Copying JAR..."
cp build/libs/*.jar /deployments/app.jar

echo "=== CUSTOM ASSEMBLE DONE ==="